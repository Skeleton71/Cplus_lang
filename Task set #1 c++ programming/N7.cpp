#include <iostream>
#include <cmath>
using namespace std;
int main()
{
  int a;
  
  cin >> a;
  cout << a << endl;
  cout << a * a * a << " " << a * a * a * a * a *a << endl;
  cout << a * a * a * a * a * a << " " << a * a * a << " " << a << endl;
  return 0;
}